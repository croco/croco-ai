services:
  shell:
    build:
      context: .
      dockerfile: Dockerfile
    image: "croco-faiss:latest"
    working_dir: "/home/croco/faiss"
    environment:
      - "LANG=ja_JP.UTF-8"
      - "LANGUAGE=ja"
    volumes:
      - $PWD/src:/home/croco/faiss
      - /models:/models
    tty: true

